# Build Log - 2026-06

## 2026-06-26 01:38 UTC - Docker image rebuild for navigation refinement

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after the navigation, Settings, Read Me, and Viewer toolbar refinements.
- Trace: branch `main`, HEAD `758f3eb`; start state intentionally dirty from ongoing Photo Viewer UI/indexing work already present in the tree.
- Commands: `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass before rebuild; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g758f3eb-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; temporary container `/api/health` smoke check pass on `127.0.0.1:4832`; `docker stop photo-viewer-rebuild-smoke-20260626-0138` pass; `git diff --check` pass.
- Result: Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g758f3eb-dirty` to image ID `sha256:70f33ac246fdde9b9d8c852da3b38d77050b6dada6793a11906a3fc80f0f4a86`.
- Repo State: intentionally dirty: this build-log entry added on top of existing frontend/server changes and generated TypeScript build info.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, rebuilt as multi-arch distribution output, installed into AppLauncher, or restarted as the active app container. Docker build reported the existing npm audit summary during production dependency prune; dependency changes were not part of this task.

## 2026-06-26 01:31 UTC - Navigation and Settings layout refinement

- Scope: Frontend UI refinement for shared navigation, Settings actions/content, and Viewer toolbar context placement.
- Trace: branch `main`, HEAD `758f3eb`; start state intentionally dirty from ongoing Photo Viewer UI/indexing work already present in the tree.
- Commands: `npm run build` pass; `git diff --check` pass; `npm run dev` started `http://localhost:5173/` and `http://localhost:3000`; local probes passed for `http://localhost:5173/` and `http://localhost:3000/api/health`.
- Result: Moved the global notification bell into the centered top navigation group after Albums; removed the Settings page user subtitle, App heading, and Storage Location card; made Read Me and Logout larger centered actions; moved Viewer gallery/folder/count context to the left side of the toolbar row; updated the Read Me Settings copy to describe Indexing instead of the removed Storage Location control.
- Repo State: intentionally dirty: `client/src/components/layout/AppChrome.tsx`, `client/src/pages/AdminPage.tsx`, `client/src/pages/ReadmePage.tsx`, `client/src/styles/global.css`, generated TypeScript build info, and this build-log entry changed on top of pre-existing dirty files.
- Notes: Build Mode only. No Docker image, AppLauncher package, release, tag, publish, live app restart, or source photo mutation was performed.

## 2026-06-26 00:27 UTC - Docker rebuild for navigation and Settings indexing UI

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after shared navigation, viewer toolbar, and Settings indexing UI changes.
- Trace: branch `main`, HEAD `758f3eb`; start state intentionally dirty from the current UI/indexing work and pre-existing clear/re-index changes.
- Commands: `git diff --check` pass; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g758f3eb-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; temporary container `/api/health` smoke check pass on `127.0.0.1:4831`; `docker stop photo-viewer-rebuild-smoke-20260625-2242` pass; `docker rm photo-viewer-rebuild-smoke-20260625-2242` pass.
- Result: Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g758f3eb-dirty` to image ID `sha256:7185e18c3c41959a942dc812d910356dc2d52ce4ccb8d4b5497be046432510d9`.
- Repo State: intentionally dirty: this build-log entry added on top of existing frontend/server changes and generated TypeScript build info.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, rebuilt as multi-arch distribution output, installed into AppLauncher, or restarted as the active app container. Docker build reported the existing npm audit summary during production dependency prune; dependency changes were not part of this task.

## 2026-06-25 22:40 UTC - Global navigation and Settings indexing UI

- Scope: Shared top navigation, viewer toolbar grouping/readability, Settings-only indexing controls, and Browse indexing action removal.
- Trace: branch `main`, HEAD `758f3eb`; start state intentionally dirty from the clear/re-index and album-related work already present in the tree.
- Commands: `npm run build` pass; `git diff --check` pass; source scan pass confirming indexing actions render only in `client/src/pages/AdminPage.tsx`; `npm run dev` started and reported `http://localhost:5173/`; sandboxed curl probes could not reach the local ports, then escalated local probes passed for `http://127.0.0.1:5173/` and `http://127.0.0.1:3000/api/health`.
- Result: Moved the full top-level menu into shared `AppChrome` for Home, Activity, Albums, notifications, user display, and Settings; added an `/activity` route for route-backed pages; split Viewer gallery/folder/count context into a separate readable toolbar while keeping photo actions centered; removed Browse indexing controls and empty-folder indexing buttons; consolidated library indexing, recursive indexing, clear-and-re-index, and clear-only actions into one Settings card with progress/error feedback.
- Repo State: intentionally dirty: `client/src/App.tsx`, `client/src/components/layout/AppChrome.tsx`, `client/src/pages/AdminPage.tsx`, `client/src/pages/AlbumsPage.tsx`, `client/src/pages/BrowsePage.tsx`, `client/src/pages/ViewerPage.tsx`, `client/src/styles/global.css`, generated build artifacts, and this build-log entry changed alongside pre-existing dirty files.
- Notes: Build Mode only. No Docker image, AppLauncher package, release, tag, publish, or multi-arch distribution artifact was created.

## 2026-06-25 21:21 UTC - Preserve albums during clear re-index

- Scope: Clear + Re-index album membership preservation, server snapshot/restore flow, indexer restore hook, and Browse action API wiring.
- Trace: branch `main`, HEAD `758f3eb`; start state clean.
- Commands: `npm run build` pass; temporary SQLite album snapshot restore check pass.
- Result: Added a server-owned clear-and-reindex endpoint that snapshots album folder memberships, explicit photo memberships, and photo exclusions by folder/file path before clearing the index, restores only entries whose folders/files still exist after the full recursive re-index, and updates the Browse `Clear + Re-index` action to use the preservation path.
- Repo State: intentionally dirty: `client/src/api/client.ts`, `client/src/pages/BrowsePage.tsx`, `server/src/admin/routes.ts`, `server/src/albums/membership-snapshot.ts`, `server/src/photos/indexer.ts`, and this build-log entry changed.
- Notes: The standalone admin `Clear Index` action still clears album contents; this change is scoped to the combined `Clear + Re-index` flow requested by the user.

## 2026-06-25 11:56 UTC - Clear and re-index failure fix

- Scope: Index clear database cleanup, indexer error progress, home-screen index action feedback, shared index progress type, local Docker image rebuild/restart, and project architecture note.
- Trace: branch `main`, HEAD `6a66703`; start state clean.
- Commands: live `docker logs --since 1h photo-viewer` clear/re-index diagnosis pass; temporary SQLite clear routine check pass; `npm run build` pass; initial sandboxed temporary SQLite check fail due tsx IPC permission, rerun with permission pass; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g6a66703-dirty .` pass; installed `photo-viewer@1.0.4` `scripts/restart.sh` pass; `/api/health` pass; Docker image/container ID match pass.
- Result: Fixed the clear-index foreign key failure by removing photo-linked activity rows before deleting photos, applied the same activity cleanup to scoped deleted-photo cleanup, exposed indexer background failures through progress state, made the home-screen indexing actions show visible error feedback instead of silently dismissing progress, rebuilt local image tags to `sha256:16e8cdf5e94ad2a4a04c362ce31a0bee868f6c42771deb7176e8f201e3261f5e`, and restarted the local AppLauncher container on `http://127.0.0.1:4820/`.
- Repo State: intentionally dirty: `client/src/pages/BrowsePage.tsx`, `packages/shared/src/api-types.ts`, `packages/shared/tsconfig.tsbuildinfo`, `server/src/admin/service.ts`, `server/src/photos/indexer.ts`, `project-dossier.md`, and this build-log entry changed.
- Notes: The live app log showed `POST /api/admin/index/clear` failed with `SQLITE_CONSTRAINT_FOREIGNKEY`; because the client only starts re-indexing after a successful clear, re-index did not start. No source photos were changed. Build Mode only: no image was pushed, tagged as `latest`, released, rebuilt as multi-arch distribution output, or git tag was created. Docker build reported the existing npm audit summary during production dependency prune; dependency changes were not part of this task.

## 2026-06-25 11:34 UTC - Docker rebuild for root indexing controls

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after root indexing controls and indexer scope semantics.
- Trace: branch `main`, HEAD `3b1a0a8`; start state intentionally dirty: root indexing control changes and prior build-log entry.
- Commands: `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g3b1a0a8-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; image file check pass for `server/dist/db/migrations/0005_photo_orientation.sql` and `client/dist/index.html`; temporary container `/api/health` host and internal probes pass; `docker stop photo-viewer-index-root-smoke-20260625-1125` pass.
- Result: Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g3b1a0a8-dirty` to image ID `sha256:7f3797dd94bcfd765e5586144cc2d647605e45ac3f5e42f96b7c6e5e65dffd16`.
- Repo State: intentionally dirty: `client/src/pages/BrowsePage.tsx`, `server/src/photos/indexer.ts`, and this build-log entry changed.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, rebuilt as multi-arch distribution output, or installed/restarted in AppLauncher. Docker build reported the existing npm audit summary during the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 11:25 UTC - Root indexing controls

- Scope: Browse-page root indexing actions and indexer scope semantics.
- Trace: branch `main`, HEAD `3b1a0a8`; start state intentionally dirty: pre-existing local build artifacts from prior frontend work.
- Commands: `npm run build` pass; `git diff --check` pass; `npm run dev` started; `curl -sSI http://127.0.0.1:5173` pass.
- Result: Made `Index Folder` and `Folder + Subfolders` visible on the library entry screen, added an admin-only confirmed `Clear + Re-index` action for clearing the index and rebuilding the library folder plus descendants, and aligned root indexer behavior so shallow and recursive root scans are distinct.
- Repo State: intentionally dirty: `client/src/pages/BrowsePage.tsx`, `server/src/photos/indexer.ts`, and this build-log entry changed.
- Notes: Original photo files are not touched by the clear/re-index flow. Browser screenshot verification was blocked because Chrome failed to launch in the sandbox and the bundled Playwright browser was not installed; frontend dev server reachability was verified by HTTP.

## 2026-06-25 10:49 UTC - Docker rebuild for index controls

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after the index reset and subtree re-index controls.
- Trace: branch `main`, HEAD `3457b6f`; start state intentionally dirty: index API/server/client changes, admin and browse UI changes, pre-existing frontend orientation/gallery layout files, generated TypeScript build metadata, and prior build-log entries.
- Commands: `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g3457b6f-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; temporary container internal `/api/health` probe pass; `docker stop photo-viewer-index-smoke-20260625-1049` pass; `docker rm photo-viewer-index-smoke-20260625-1049` pass.
- Result: Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g3457b6f-dirty` to image ID `sha256:a78cc07c6945a189e995cb8f3bf892f4f986619f0f8fad0910be2ff9268208e8`; smoke container returned `200` from `/api/health`.
- Repo State: intentionally dirty: build-log entry added on top of existing implementation changes and generated TypeScript build metadata; Docker image artifact updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, rebuilt as multi-arch distribution output, or installed/restarted in AppLauncher. Docker build reported the existing npm audit summary during the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 10:42 UTC - Index reset and subtree re-index controls

- Scope: Admin index clearing API/UI, folder subtree re-index request flow, scoped index cleanup behavior, shared index API types, and production build verification.
- Trace: branch `main`, HEAD `3457b6f`; start state intentionally dirty: pre-existing frontend orientation/gallery layout files, generated TypeScript build metadata, and prior build-log entries.
- Commands: `npm run build` pass; `git diff --check` pass.
- Result: Added an admin-only Clear Index action that refuses to run during active indexing and clears indexed folders/photos, search data, caches, album membership rows, and photo-linked records without touching source photo files; added a folder-level `Folder + Subfolders` re-index option; normalized index targets to stay inside the configured library; limited scoped PSD preview generation to the indexed scope; rebuilt folder stats and search after stale-row cleanup.
- Repo State: intentionally dirty: index API/server/client files, admin and browse UI files, shared API types, generated TypeScript build metadata, this build-log entry, plus pre-existing unrelated frontend changes.
- Notes: Browser visual verification was not run in this slice; production build and static diff checks passed. No Docker image, AppLauncher package, release tag, published artifact, source photo mutation, or live app data reset was performed.

## 2026-06-25 07:09 UTC - Docker image rebuild for gallery overflow fix

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after the gallery horizontal overflow clamp and current dirty frontend changes.
- Trace: branch `main`, HEAD `3457b6f`; start state intentionally dirty: pre-existing thumbnail orientation/viewer changes, gallery overflow layout fix, generated TypeScript build metadata, and build-log entries.
- Commands: `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g3457b6f-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; temporary container `/api/health` probe pass on host port `4844`; `docker stop photo-viewer-rebuild-smoke-20260625-0709` pass.
- Result: Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g3457b6f-dirty` to image ID `sha256:93ed4e8a41084d75baf76a2e035b6a7a42ba61e5f360208c6e73c10ed27f9305`.
- Repo State: intentionally dirty: build-log entry added on top of existing frontend/layout changes and generated TypeScript build metadata; Docker image artifact updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. Docker build reported the existing npm audit summary during the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 07:05 UTC - Gallery horizontal overflow clamp

- Scope: Browse gallery horizontal overflow fix for virtualized thumbnail rows and long photo filenames.
- Trace: branch `main`, HEAD `8569eb0`; start state intentionally dirty: pre-existing thumbnail orientation/viewer changes, generated TypeScript build metadata, and prior build-log entries.
- Commands: `npm run build` pass; Chrome verification pass on `http://localhost:5173/` dense photo folder with no document/body horizontal overflow before or after vertical scrolling.
- Result: Clamped the browse page and thumbnail grid to the viewport width, changed virtualized photo rows to `minmax(0, 1fr)` tracks, and allowed shared photo/folder card shells to shrink under long filenames without widening the page.
- Repo State: intentionally dirty: layout fix files, generated build metadata from the production build, pre-existing unrelated frontend changes, and this build-log entry changed.
- Notes: Vertical gallery scrolling remains enabled; horizontal page/content scrolling is hidden. No Docker image, AppLauncher package, release tag, or published artifact was created.

## 2026-06-25 06:52 UTC - Orientation save on viewer exit

- Scope: Viewer orientation save robustness, cached photo orientation consistency, production build, and local Docker image rebuild for Photo Viewer 1.0.4.
- Trace: branch `main`, HEAD `8569eb0`; start state intentionally dirty: pre-existing thumbnail orientation work, generated TypeScript build metadata, and this build log already modified.
- Commands: `npm run build` pass; `git diff --check` pass; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g8569eb0-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; image client bundle check pass; temporary container `/api/health` probe pass on host port `4843`; `docker stop photo-viewer-orientation-save-smoke-20260625-0645` pass.
- Result: Viewer exit now waits for any pending orientation save before leaving; rotate saves use `keepalive`; orientation updates are applied to cached folder, search, album, photo-detail, and On This Day photo data and invalidated after a successful save so reopening from the gallery does not use stale orientation. Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g8569eb0-dirty` to image ID `sha256:17ebb26508eb5a531be0aa85dcd5d7d2b75fc25be730a003a3446860a410efb4`.
- Repo State: intentionally dirty: viewer orientation save fix, pre-existing thumbnail orientation component/usages, generated TypeScript build metadata, and this build-log entry changed.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. Docker build reported the existing npm audit summary from the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 06:32 UTC - Thumbnail orientation display

- Scope: Oriented thumbnail rendering for saved photo rotations, production build, and local Docker image rebuild for Photo Viewer 1.0.4.
- Trace: branch `main`, HEAD `8569eb0`; start state intentionally dirty: pre-existing viewer orientation implementation, UI styling files, generated TypeScript build metadata, and this build log already modified.
- Commands: `npm run build` pass; `git diff --check` pass; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g8569eb0-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; image client bundle check pass; temporary container `/api/health` probe pass on host port `4842`; `docker stop photo-viewer-thumbnail-orientation-smoke-20260625-0625` pass.
- Result: Added a reusable oriented thumbnail renderer and applied saved `orientationDeg` to the viewer thumbnail strip, browse/search photo cards, album photo tiles, and On This Day thumbnails. Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g8569eb0-dirty` to image ID `sha256:061d4f62f3d320b73992101f0717cc40382820086d8a5ac7aabb68875cd83b20`.
- Repo State: intentionally dirty: thumbnail orientation component and usages, prior viewer orientation implementation files, generated TypeScript build metadata, and this build-log entry changed.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. Docker build reported the existing npm audit summary from the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 06:15 UTC - Viewer rotation and metadata field refinement

- Scope: Viewer rotate controls, persisted photo orientation, right-panel notification/follow placement, people/comment empty-field styling, production build, and local Docker image rebuild for Photo Viewer 1.0.4.
- Trace: branch `main`, HEAD `8569eb0`; start state intentionally dirty: pre-existing viewer/albums UI files, generated TypeScript build metadata, and this build log already modified.
- Commands: `npm run build` fail first because album viewer also needed the new `onPhotoUpdate` prop; `npm run build` pass after patch; `git diff --check` pass; built server migration package check pass for `0005_photo_orientation.sql`; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g8569eb0-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; image migration-file check pass; temporary container `/api/health` probe pass on host port `4841`; `docker stop photo-viewer-rotation-smoke-20260625-0615` pass.
- Result: Added clockwise and counter-clockwise rotate buttons to the viewer toolbar; persisted quarter-turn orientation in SQLite via `orientation_degrees`; applied saved orientation to the displayed image with container-aware sizing; passed orientation through browse, album, search, and On This Day photo responses; moved the photo follow notification bell onto the emoji reaction row with a larger icon target; changed People and Comments empty states to use the same metadata card styling; rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g8569eb0-dirty` to image ID `sha256:384337525da0151aa0466134f11b962b694960061322e9ac9c2ed572b1c2b95d`.
- Repo State: intentionally dirty: viewer UI files, shared/server orientation implementation files, new migration `server/src/db/migrations/0005_photo_orientation.sql`, generated TypeScript build metadata, pre-existing unrelated UI changes, and this build-log entry changed.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. Docker build reported the existing npm audit summary from the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 05:33 UTC - Viewer actions and metadata affordance

- Scope: Viewer action toolbar styling, add-to-album viewer action, metadata inspector field affordance, production build, and local Docker image rebuild for Photo Viewer 1.0.4.
- Trace: branch `main`, HEAD `8569eb0`; start state intentionally dirty: pre-existing `docs/build-logs/2026-06.md` entry only.
- Commands: `npm run build` pass; `git diff --check` pass; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g8569eb0-dirty .` pass; `docker image inspect ...:1.0.4` pass; temporary smoke container `/api/health` probe pass; `docker stop photo-viewer-ui-smoke-20260625-0630` pass.
- Result: The viewer slideshow, download, album, and information actions now sit in a brighter bounded toolbar with larger icon targets; the viewer includes an add-to-album action backed by the existing album picker; metadata fields now render as highlighted editable boxes with stronger empty-field prompts. Rebuilt numbered local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g8569eb0-dirty` to image ID `sha256:b9563e20e7ffe8f1106cc9d3e8a031367561b0913cb5b21b9e4c6e823aa46077`.
- Repo State: intentionally dirty: viewer UI implementation files plus pre-existing and new build-log entries; Docker image artifact updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. Docker build reported the existing npm audit summary from the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 05:19 UTC - Docker rebuild for current app

- Scope: Local Docker image rebuild and running container recreation for Photo Viewer 1.0.4 at current commit `8569eb0`.
- Trace: branch `main`, HEAD `8569eb0`; start state clean.
- Commands: `docker ps -a --filter name=photo-viewer` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g8569eb0` confirmed missing before rebuild; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g8569eb0 .` pass; installed AppLauncher `photo-viewer/1.0.4` restart script pass; `docker inspect photo-viewer` pass; `curl -sS http://127.0.0.1:4820/api/health` pass; `docker exec photo-viewer ...` pass for `0004_album_photo_exclusions.sql` and client build presence.
- Result: Rebuilt the local numbered image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g8569eb0`; recreated the running `photo-viewer` container through the installed AppLauncher package; container now runs image ID `sha256:9a74195f4c2011f3b3d58c584341366071513a363b59f903615355ad79444b37` on host port `4820`.
- Repo State: intentionally dirty: this build-log entry added; Docker image/container artifacts updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. Docker build reported the existing npm audit summary from the production dependency prune step; dependency changes were not part of this task.

## 2026-06-25 05:10 UTC - Album header and hover action refinement

- Scope: Album workspace header identity, sidebar create-album control text, album photo hover actions, and album UI styling.
- Trace: branch `main`, HEAD `3c11c4c`; start state intentionally dirty: existing album UI/API/server implementation files, generated client TypeScript build info, migration file, and prior build-log entries.
- Commands: `npm run build` pass; `git diff --check` pass; local `/api/health` probe pass; local `/albums` frontend probe pass; `rg` UI-string/control check pass; Chrome automation not run because Chrome aborted from the sandboxed Playwright launcher and the bundled Chromium binary was not installed.
- Result: Album page top-left chrome now shows `Photo Viewer`, version `v1.0.4`, and the theme toggle together; the top-left `Albums` navigation pill was removed; the left album rail heading was removed; the create action now reads `New Album`; editable album photo hover actions now show both add-to-album and remove-from-album controls.
- Repo State: intentionally dirty: album UI/API/server implementation files, `server/src/db/migrations/0004_album_photo_exclusions.sql`, generated client TypeScript build info, and this build-log entry changed.
- Notes: Build Mode only. No Docker image, AppLauncher package, release tag, published artifact, or app data mutation was created for this refinement.

## 2026-06-25 04:53 UTC - Album photo remove and Docker rebuild

- Scope: Album-page photo removal, album photo viewer entry, folder-derived album photo exclusions, DELETE request handling, and local Docker image rebuild for Photo Viewer 1.0.4.
- Trace: branch `main`, HEAD `3c11c4c`; start state intentionally dirty: prior combined-albums implementation files, generated client TypeScript build info, and previous build-log entries.
- Commands: `npm run build` pass; isolated album service check pass for folder-derived photo removal and add-back exclusion clearing; `git diff --check` pass; Chrome verification pass on `http://localhost:5173/albums/codex-temp-album-remove-ui` with a temporary local album; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g3c11c4c-dirty .` pass; Docker image inspect pass; temporary container `/api/health` smoke check pass; image migration-file check pass for `0004_album_photo_exclusions.sql`; `docker stop photo-viewer-album-remove-smoke-20260625-0452` pass.
- Result: Album photos now open the same full viewer used by library photos, editable album photos reveal a hover remove action, folder-derived removed photos are hidden through `album_photo_exclusions`, album data refreshes immediately after removal, empty-body DELETE requests no longer send a JSON content type, and numbered Docker tags `1.0.4`, `1.0`, `1`, and `1.0.4-g3c11c4c-dirty` now point to image ID `f78422929254`.
- Repo State: intentionally dirty: album UI/API/server implementation files, `server/src/db/migrations/0004_album_photo_exclusions.sql`, generated client TypeScript build info, and this build-log entry changed; temporary local verification album was removed from `server/data/photo-viewer.db`; Docker image artifact updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. The existing running `photo-viewer` container was not restarted and still reports image `961486115292`; only the image tags were rebuilt. Browser verification used local indexed photo records whose original files are not all mounted, so thumbnail generation logged expected missing-file errors during the temporary-album check.

## 2026-06-25 05:55 UTC - Viewer metadata defaults and Docker rebuild

- Scope: Viewer right-side metadata panel defaults, right-panel field styling consistency, top-right notification bell prominence, and local Docker image rebuild.
- Trace: branch `main`, HEAD `8569eb0`; start state intentionally dirty with pre-existing viewer/albums UI files and this build log already modified.
- Commands: `npm run build` pass; `git diff --check` pass; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g8569eb0-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; temporary container `/api/health` smoke check pass on host port `4839`; `docker stop photo-viewer-ui-smoke-20260625-0900` pass; `docker rm photo-viewer-ui-smoke-20260625-0900` pass.
- Result: Metadata now opens by default in both gallery and album viewer entry paths, the information button toggles the panel off and on, metadata/tag/comment inputs use consistent right-panel field styling, and the notification bell uses a larger highlighted icon target with a stronger unread badge. Rebuilt local image tags `1.0.4`, `1.0`, `1`, and `1.0.4-g8569eb0-dirty` to image ID `b07b64ea57cc`.
- Repo State: intentionally dirty: viewer UI implementation files, generated TypeScript build output metadata, pre-existing unrelated UI changes, and this build-log entry changed.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. No local viewer app was already running on ports `5173` or `4820`; visual browser verification was not performed.

## 2026-06-25 22:16 UTC - Docker image rebuild for app chrome update

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after app chrome, Settings, search/taskbar, Viewer toolbar, and current backend changes.
- Trace: branch `main`, HEAD `758f3eb`; start state intentionally dirty with current frontend/backend/build-log work.
- Commands: `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g758f3eb-dirty .` failed before app build on Docker Hub syntax image `DeadlineExceeded`; `DOCKER_BUILDKIT=0 docker build ...` was stopped after hanging without progress; `npm run build` pass; fallback staged from existing `ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4`, copied fresh `packages/shared/dist`, `server/dist`, and `client/dist`, `docker commit` pass, numbered `docker tag` commands pass; temporary container `/api/health` smoke check pass; image migration-file check pass through `0005_photo_orientation.sql`.
- Result: Rebuilt the local `ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` image and moved numbered tags `1.0.4`, `1.0`, `1`, and trace tag `1.0.4-g758f3eb-dirty` to image ID `5f711685760c`.
- Repo State: intentionally dirty: build output, frontend/backend source changes, and this build-log entry; Docker image artifact updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, installed, or rebuilt as multi-arch distribution output. Temporary staging and smoke containers were removed after verification.

## 2026-06-25 21:53 UTC - App chrome and viewer toolbar refinement

- Scope: Browser UI chrome, browse taskbar, indexing controls, Settings links, search placement, and Viewer action toolbar.
- Trace: branch `main`, HEAD `758f3eb`; start state intentionally dirty with pre-existing album/backend/build-log work.
- Commands: `npm run build` pass in `client`; `git diff --check` pass.
- Result: Added shared authenticated app chrome with app name, `v1.0.4`, theme control, and centered Home action; moved browse search into a centered taskbar above the scrollable photo/folder area; consolidated the three indexing actions into a dropdown; moved Read Me and Logout into Settings; made Settings accessible to non-admin users while keeping admin tools restricted; removed the Viewer label and filename header; moved Gallery, folder context, and photo actions into one Viewer toolbar row.
- Repo State: intentionally dirty: frontend UI files and this build-log entry changed; unrelated pre-existing source/doc changes preserved.
- Notes: Build Mode only. No Docker image, AppLauncher package, release tag, or published artifact was created.

## 2026-06-25 04:28 UTC - Docker image rebuild for album UI

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after the combined albums workspace UI changes.
- Trace: branch `main`, HEAD `3c11c4c`; start state intentionally dirty: album UI implementation files, generated client TypeScript build info, and prior build-log entry.
- Commands: `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g3c11c4c-dirty .` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; temporary container `/api/health` smoke check pass; image migration-file check pass for `0003_albums.sql`; `docker stop photo-viewer-rebuild-smoke-20260625-0428` pass.
- Result: Rebuilt the local `ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` image and moved numbered tags `1.0.4`, `1.0`, `1`, and trace tag `1.0.4-g3c11c4c-dirty` to image ID `961486115292`.
- Repo State: intentionally dirty: this build-log entry added on top of existing album UI changes; Docker image artifact updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, released, or rebuilt as multi-arch distribution output. The temporary smoke container was removed after verification.

## 2026-06-25 04:17 UTC - Combined albums workspace UI

- Scope: Album route consolidation, split-view album management layout, resolved-photo display, top-menu continuity, and photo/folder hover focus refinement.
- Trace: branch `main`, HEAD `0b407e8`; start state intentionally dirty: pre-existing album implementation/build artifacts from earlier work.
- Commands: `npm run build` pass; `git diff --check` pass; Chrome verification pass on `http://localhost:5173/albums` and `/albums/codex-temp-album-ui` with a temporary local album, then cleanup pass.
- Result: Routed `/albums` and `/albums/:albumId` to one combined albums screen with a narrow left album list and right-side detail panel, kept top navigation on album routes, moved album name/visibility/delete/save controls into the fixed detail header, reduced the content section to resolved photos under the `Photos` label, removed the old source-folder/detail sections, removed the redundant photo hover filename tooltip, and strengthened visible hover/focus treatment for album rows, photos, and folders.
- Repo State: intentionally dirty: frontend implementation files, generated client TypeScript build info, and this build-log entry changed; temporary local verification album was removed from `server/data/photo-viewer.db`.
- Notes: Browser verification found no console errors. No Docker image, AppLauncher package, release tag, or published artifact was created.

## 2026-06-25 04:00 UTC - Docker artifact cleanup

- Scope: Local Docker cleanup for stale Photo Viewer containers and image tags.
- Trace: branch `main`, HEAD `0b407e8`; start state intentionally dirty: prior Docker rebuild build-log entry.
- Commands: `docker ps -a --filter name=photo-viewer` pass; `docker image ls ... | grep 'photo-viewer|<none>'` pass; `docker volume ls ... | grep photo-viewer` pass; `docker rm 2b8ec762d088 dcff5dafc627 ed560c39c002` pass; `docker rmi ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g44093e1-dirty ghcr.io/paulmarshall-wfw/photo-viewer:1.0.3 photo-viewer:1.0.3 photo-viewer:1.0.2 ghcr.io/paulmarshall-wfw/photo-viewer:1.0.1 photo-viewer:1.0.1 ghcr.io/paulmarshall-wfw/photo-viewer:1.0.0 photo-viewer:1.0.0` pass.
- Result: Removed three stopped legacy Photo Viewer containers and stale Photo Viewer image tags for `1.0.0`, `1.0.1`, `1.0.2`, `1.0.3`, and the duplicate dirty trace tag on the current image.
- Repo State: intentionally dirty: build-log entry only; Docker artifacts changed outside Git.
- Notes: Preserved the `photo-viewer-production-data` volume and the running `photo-viewer` container. The running container still uses image ID `5912b8a60eea`, so its backing image/tag was not removed.

## 2026-06-25 03:57 UTC - Album image Docker rebuild

- Scope: Local Docker image rebuild for Photo Viewer 1.0.4 after the virtual albums implementation.
- Trace: branch `main`, HEAD `0b407e8`; start state clean.
- Commands: `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0 -t ghcr.io/paulmarshall-wfw/photo-viewer:1 -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g44093e1-dirty .` pass; `docker tag ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4-g0b407e8` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; image migration-file check pass for `0003_albums.sql`; temporary container `/api/health` smoke check pass; `docker stop photo-viewer-albums-smoke` pass.
- Result: Rebuilt the local `ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` image, applied numbered family and trace tags, verified the album migration is present inside the image, and confirmed the container health endpoint returns `200`.
- Repo State: intentionally dirty: this build-log entry only; Docker image artifact updated outside Git.
- Notes: Build Mode only. No image was pushed, tagged as `latest`, or released. The installed AppLauncher `photo-viewer/1.0.4` package already points at `IMAGE_TAG=1.0.4`, so reinstall is not required; restart the AppLauncher package to run the rebuilt image.

## 2026-06-25 03:06 UTC - Virtual albums implementation

- Scope: Database-backed virtual albums, album API authorization, shared album contracts, Albums UI routes, folder/photo album picker controls, and server build migration packaging.
- Trace: branch `main`, HEAD `44093e1`; start state intentionally dirty: pre-existing untracked `docs/plans/` plan files.
- Commands: `npm run build` pass before and after server build-script migration copy update; fresh SQLite migration check pass with `0001_initial.sql`, `0002_social_features.sql`, and `0003_albums.sql`; existing-DB migration check pass applying only `0003_albums.sql`; Fastify inject album API check pass for `401`, private-read blocking, shared-read/non-owner-edit authorization, add/remove membership, and direct-plus-folder resolved-photo dedupe; Chrome UI check pass for login, Albums nav/list/create/detail, folder picker, photo picker, dark mode, no detected horizontal overflow, and no console errors; thumbnail endpoint check pass with temporary JPEG fixture; `git diff --check` pass.
- Result: Added album tables, schema, routes, service logic, shared/client types, React Query hooks, `/albums` and `/albums/:albumId` pages, owner/admin edit controls, visible album picker actions for folder/photo cards, and build output copying for SQL migrations.
- Repo State: intentionally dirty: album implementation files, `server/package.json`, generated TypeScript build info, this build-log entry, and pre-existing untracked `docs/plans/`.
- Notes: Chrome verification used a temporary database and local servers on `127.0.0.1`; touch/mobile access is covered by the picker CSS coarse-pointer fallback but was not separately device-emulated.

## 2026-06-25 02:05 UTC - Browse layout and AppLauncher rebuild

- Scope: Browse page folder/photo layout, empty-folder display filtering, app version metadata, local Docker image, and installed AppLauncher package.
- Trace: branch `main`, HEAD `c8bdfce`; start state clean.
- Commands: `npm install --package-lock-only --ignore-scripts` pass with existing audit warnings; `npm run build` pass; `docker build -t ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4 ...` fail before app build on Docker Hub syntax image `DeadlineExceeded`; `DOCKER_BUILDKIT=0 docker build ...` stopped after hanging during context/base processing; local image rebuild from existing `ghcr.io/paulmarshall-wfw/photo-viewer:1.0.3` base via `docker create`, `docker cp`, `docker commit`, and numbered `docker tag` commands pass; `npm run applauncher:install` pass; `npm run applauncher:validate` pass; installed `photo-viewer@1.0.4` `scripts/doctor.sh` pass; installed `photo-viewer@1.0.4` `scripts/health.sh` pass; `docker image inspect ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` pass; `git diff --check` pass.
- Result: Suppressed zero-photo folders in the browse grid, added a divider between displayed folders and photos, corrected thumbnail-grid virtualization offset below preceding page content, bumped local app/package metadata to `1.0.4`, built a local `ghcr.io/paulmarshall-wfw/photo-viewer:1.0.4` image with numbered family/trace tags, and installed the active AppLauncher package at `~/Library/Application Support/AppLauncher/launch-packages/photo-viewer/1.0.4/`.
- Repo State: intentionally dirty: implementation, version metadata, package lock, AppLauncher docs/config, and this build-log entry changed; ignored machine-local `config/applauncher.env` updated to `IMAGE_TAG=1.0.4`; generated `dist/` package and Docker image updated outside Git.
- Notes: No image was pushed or released. The currently running `photo-viewer` container was left on `ghcr.io/paulmarshall-wfw/photo-viewer:1.0.3` so the user can restart it from AppLauncher and pick up `1.0.4`.

## 2026-06-25 00:43 UTC - AppLauncher photo library mount path

- Scope: Photo Viewer machine-local AppLauncher config and installed package mount verification.
- Trace: branch `main`, HEAD `10280a7`; start state intentionally dirty: pre-existing AppLauncher wrapper PATH change in `scripts/applauncher/generate-launch-package.mjs` and build-log entry.
- Commands: `test -d "/Volumes/3TB Disk 001/BigTony_01"` pass; `npm run applauncher:validate` pass; installed `photo-viewer@1.0.3` `scripts/doctor.sh` pass; installed `photo-viewer@1.0.3` `scripts/start.sh` pass; `docker inspect photo-viewer --format '{{json .Mounts}}'` pass; `git diff --check` pass; build-log check pass.
- Result: Updated ignored `config/applauncher.env` to use `LIBRARY_PATH=/Volumes/3TB Disk 001/BigTony_01`. The installed AppLauncher package starts successfully and Docker reports `/library` mounted read-only from that host folder.
- Repo State: intentionally dirty: pre-existing source wrapper generator/build-log changes plus this build-log entry; ignored machine-local `config/applauncher.env` updated and preserved outside Git.
- Notes: No image was built or published; this stayed in local Build Mode. Secret value was not recorded in the build log.

## 2026-06-24 10:56 UTC - AppLauncher local launch config

- Scope: AppLauncher machine-local config, generated package wrappers, installed `photo-viewer` 1.0.3 launch package, and Docker CLI discovery for GUI-launched package operations.
- Trace: branch `main`, HEAD `10280a7`
- Commands: `find /Users/paulmarshall/Pictures -maxdepth 2 -type d` pass; `node -e "crypto randomBytes"` pass; `dist/applauncher-launch-packages/photo-viewer/1.0.3/scripts/doctor.sh` pass; `npm run applauncher:install` pass; `npm run applauncher:validate` pass; `env -i ... launch-packages/photo-viewer/1.0.3/scripts/doctor.sh` pass.
- Result: Created ignored `config/applauncher.env` with `IMAGE_TAG=1.0.3`, `PORT=4820`, readable `LIBRARY_PATH=/Users/paulmarshall/Pictures`, and a generated 32+ character `SESSION_SECRET`. Updated generated AppLauncher wrappers to prepend standard macOS Docker CLI paths, then regenerated and installed the `photo-viewer` 1.0.3 package. Installed package doctor now reports ready under a stripped-down environment.
- Repo State: intentionally dirty: source wrapper generator and this build log changed; ignored machine-local `config/applauncher.env` created and preserved outside Git.
- Notes: No image was built or published; this stayed in local Build Mode. Secret value was not recorded in the build log.

## 2026-06-24 10:40 UTC - AppLauncher 2.0 launch package scripts

- Scope: AppLauncher launch-package generation, package operation scripts, target-owned config docs, Docker compose launch environment, and local port documentation.
- Trace: branch `main`, HEAD `9b425ce`
- Commands: `npm run applauncher:generate` pass; `npm run applauncher:validate` pass; `python3 "/Users/paulmarshall/Software Development/All Standards/scripts/check-local-port-registry.py"` pass; `node "/Users/paulmarshall/Software Development/All Skills/target-app-launch-package/scripts/validate_launch_package.mjs" --package "/Users/paulmarshall/Library/Application Support/AppLauncher/launch-packages/photo-viewer/1.0.3" --run-doctor` pass; installed `scripts/health.sh` pass with expected `warning` report because the app is not running; `git diff --check` pass; `npm run build` pass.
- Result: Added target-owned AppLauncher 2.0 package generation, package-relative `start`, `stop`, `restart`, `health`, and `doctor` scripts, redacted config/secret checks, docs, and an installed active package at `~/Library/Application Support/AppLauncher/launch-packages/photo-viewer/1.0.3/`. Removed legacy installed `photo-viewer` manifest roots from `manifest-install/` and `manifests/`. Registered host port `4820` in the shared local port registry.
- Repo State: intentionally dirty: pre-existing `HANDOFF.md` modification and untracked `project-dossier.md` preserved; AppLauncher package work adds/updates `.gitignore`, `AGENTS.md`, `config/applauncher.env.example`, `deploy/docker-compose.yml`, `docs/applauncher-launch-package.md`, `docs/build-logs/2026-06.md`, `package.json`, and `scripts/applauncher/`.
- Notes: `doctor` validates report shape without printing values and currently reports missing machine-local `LIBRARY_PATH`, missing/placeholder `SESSION_SECRET`, and Docker daemon unavailable. Create `config/applauncher.env` before using `start`.
